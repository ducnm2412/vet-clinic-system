# Eureka Server

Service discovery — nơi các service khác tự đăng ký khi khởi động; API Gateway và các service tra cứu địa chỉ nhau qua đây thay vì cấu hình IP/port cố định.

- Công nghệ: Spring Cloud Netflix Eureka
- Database: không có
- Cấu trúc dự kiến: `src/main/java/...`, `application.yml`, `Dockerfile`
